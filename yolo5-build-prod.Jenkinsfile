pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'ls'
                sh 'echo building yolov prod pipeline 2 ...'
            }
        }
    }
}
